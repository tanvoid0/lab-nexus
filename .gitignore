# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
# This project uses pnpm only (see package.json "packageManager").
package-lock.json
yarn.lock
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage
/playwright-report/
/test-results/
/blob-report/

# uploaded assets (keep directory)
/public/uploads/*
!/public/uploads/.gitkeep

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files — never commit secrets (template is committed as `.env.example`)
.env
.env.*
!.env.example
# Written on first local `pnpm db:seed` when SEED_DEMO_PASSWORD is unset (random demo password)
prisma/.seed-demo-credentials.json

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts
*.xlsx
*.csv