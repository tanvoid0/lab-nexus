/** Next.js `unstable_cache` revalidation for user settings (seconds). */
export const USER_SETTINGS_SERVER_CACHE_SECONDS = 60 * 60; // 1 hour

/** Client hook skips background refresh while data is newer than this (ms). */
export const USER_SETTINGS_CLIENT_TTL_MS = 60 * 60 * 1000; // 1 hour
