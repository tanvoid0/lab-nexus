/** Fixed Mongo `_id` for the singleton lab currency document. */
export const LAB_CURRENCY_CONFIG_ID = "lab_currency_config";

/** Max extra transaction currencies (beyond functional) to keep lists manageable. */
export const MAX_ADDITIONAL_TRANSACTION_CURRENCIES = 40;
